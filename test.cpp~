#include<queue>
#include<iostream>
using namespace std;
int main()
{
	queue<pair<int,int> > a;
	for(int i=0;i<100;i++)
		if(i%2==0){
			pair<int,int>tmp(i,i);
			a.push(tmp);
			cout<<a.size()<<endl;
		}
	cout<<a.front().first;
	return 0;
}

